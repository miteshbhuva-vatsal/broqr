rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {

    // Allow any authenticated user to read/write any path (relaxed for testing)
    match /{allPaths=**} {
      allow read: if true;
      allow write: if request.auth != null;
    }
  }
}
